// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set this to your deployed URL before going live (used for canonical links/sitemaps later)
  // site: 'https://example.com',
});
